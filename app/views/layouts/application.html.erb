<!DOCTYPE html>
<html>
<head>
  <title>Stackoverflow</title>
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <nav class="navbar navbar-default" role="navigation">
    <div class="container-fluid">
      <div class="navbar-header">
        <%= link_to "Stack Overflow", root_path, class: "navbar-brand" %>
      </div>
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav">
          <li><%= link_to "Questions", questions_path %></li>
          <li><%= link_to "Users", users_path %></li>
          <% if current_user %>
            <li><%= link_to "New Question", new_user_question_path(current_user) %></li>
          <% end %>
        </ul>
        <% if current_user %>
          <%= link_to "Logout", session_path("current"), method: "delete", class: "pull-right" %>
          <span class="pull-right">Logged in as <%= current_user.name %>.</span>
        <% else %>
          <%= link_to "Sign up", signup_path, class: "pull-right" %>
          <%= link_to "Login", new_session_path, class: "pull-right" %>
        <% end %>
      </div>
    </div>
  </nav>
  <div class="container">
    <%= flash[:alert] %>
    <%= flash[:notice] %>
    <%= yield %>
  </div>
</body>
</html>
